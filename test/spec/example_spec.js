define(
	[],
	function() {

		describe("Unit Testing", function() {
			it("is fun", function() {
				expect(1);
			});

			it("is good for you", function() {
				expect(1);
			});

			it("can make you a better programmer", function() {
				expect(1);
			});
		});

});